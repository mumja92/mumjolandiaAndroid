package com.mumja.tetris.board

enum class BlockColor {
    BLUE,
    CYAN,
    GREEN,
    MAGNETA,
    ORANGE,
    PINK,
    RED,
    YELLOW,
    LIGHT_GRAY,  //last color is for empty blocks; it shouldn't be picked by accident
}